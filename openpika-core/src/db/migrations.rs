// SQLx compile-time migration embedding is handled by the sqlx::migrate!() macro
// in db/mod.rs pointing at the `migrations/` directory.
// This module is reserved for any runtime migration helpers.
